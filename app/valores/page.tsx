import Valores from "@/components/valores";

const page = () => {
  return (
    <section className="h-screen bg-[#0d1018] flex items-center justify-center">
      <Valores />
    </section>
  );
};

export default page;
